# Hello, it's me!

This program prompts the user for their name and then greets them by name.

If the user's name is Adele, the output will be `hello, Adele\n`.

It uses the `get_string()` function declared in `cs50.h`.

It also uses the formatting tool `%s` in the function `printf()`.
